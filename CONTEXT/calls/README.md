# CONTEXT/calls — call data (git-ignored)

- `inbox/`     drop new YAML exports here (transcript + AI feedback). Triggers analysis.
- `analyzed/`  processed YAMLs + their analysis notes. Never deleted.
- `playbook.md` running coaching memory (recurring issues, improvements, guidance).
- `.processed` ledger of filenames already handled, so nothing is analyzed twice.

This is data, not OS — it stays out of GitHub.
